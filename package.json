{
  "name": "online-ticket-booking-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf public && mkdir -p public && cp -R frontend/dist/* public/"
  }
}
